# Run this as root, because the web server needs to have privileges to access the HTTPS port
command="python -u synth/web_to_zeromq.py"
echo $command
pkill -f "$command"
nohup $command >> ../synth_logs/web_to_zeromq.out 2>&1 &
